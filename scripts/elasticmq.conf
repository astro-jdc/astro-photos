include classpath("application.conf")
node-address { protocol = http, host = "*", port = 9324, context-path = "" }
rest-sqs { enabled = true, bind-port = 9324, bind-hostname = "0.0.0.0" }
queues {
  astro-photos-dev-ingest      { defaultVisibilityTimeout = 300 seconds }
  astro-photos-dev-reconstruct { defaultVisibilityTimeout = 900 seconds }
}
