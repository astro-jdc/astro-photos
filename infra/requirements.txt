# astro-photos — infraestructura (AWS CDK v2, Python 3.12)
#
#   /usr/bin/python3.12 -m venv infra/.venv
#   infra/.venv/bin/pip install -r infra/requirements.txt
#
# El CLI de CDK es un binario de Node; `aws-cdk-cli` es el paquete oficial de
# PyPI que lo empaqueta, así que instalando esto queda `infra/.venv/bin/cdk`
# disponible y `make synth` funciona sin tocar npm. La version esta fijada a
# proposito: el CLI tiene que ser >= la version de la libreria de constructos.

aws-cdk-lib>=2.170.0,<3.0.0
aws-cdk-cli==2.1139.0
constructs>=10.3.0,<11.0.0
cdk-nag>=2.28.0
boto3>=1.43.84

# tests
pytest>=8.3
pytest-cov>=6.0
PyYAML>=6.0
